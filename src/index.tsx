import * as React from 'react'
import  ObserverComponent from "./stateComponents/observer";

export const Observer = (props: any) =>  (<ObserverComponent {...props} />)
export const TestObserver = (props: any) =>  (<ObserverComponent {...props} />)


